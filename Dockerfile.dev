FROM node:22-alpine

# Security updates and essential packages
RUN apk update && apk upgrade && \
    apk add --no-cache \
    git \
    bash \
    curl \
    unzip \
    ca-certificates \
    && rm -rf /var/cache/apk/*

# Create non-root user
RUN addgroup -g 1010 claude && \
    adduser -D -s /bin/bash -u 1010 -G claude claude

# Install Claude Code globally
RUN npm install -g @anthropic-ai/claude-code

# Copy entrypoint script
COPY docker/entrypoint-dev.sh /usr/local/bin/entrypoint.sh
RUN chmod +x /usr/local/bin/entrypoint.sh

# Create npm cache directory with correct permissions
RUN mkdir -p /home/claude/.cache/npm /home/claude/.cache/npm/_logs && 
    chown -R claude:claude /home/claude/.cache/npm

# Create config directory for claude user
RUN mkdir -p /home/claude/.config && 
    chown -R claude:claude /home/claude/.config

# Set npm cache location explicitly
ENV NPM_CONFIG_CACHE=/home/claude/.cache/npm

# Set working directory
WORKDIR /var/www/html

# Switch to non-root user
USER claude

# Set entrypoint
ENTRYPOINT ["/usr/local/bin/entrypoint.sh"]