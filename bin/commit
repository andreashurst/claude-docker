#!/bin/bash
#
# Simple wrapper for git-commit-ai
# Usage: commit [options]
#

# Use the full path to git-commit-ai
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/git-commit-ai" "$@"
