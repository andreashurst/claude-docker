# Claude Dev Environment
PS1='\[\033[01;32m\]claude@dev\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '

export PATH="/var/www/html/bin:/usr/local/bin:$PATH"
export NODE_PATH="/usr/local/lib/node_modules:$NODE_PATH"
alias ll='ls -la'
alias ..='cd ..'
alias ...='cd ../..'
alias test-connectivity='/home/claude/.claude/scripts/test-connectivity.sh'
alias commit='git-commit-ai'
alias mcp='mcp-status'
alias gs='git status'
alias gd='git diff'
alias gl='git log --oneline -10'
alias gb='git branch'
alias gf='git fetch'
alias health='claude-health'

# Block system package manager
alias apk='echo "Use the host system for APK package management" && false'

# Auto-start claude if available
if [ -t 1 ]; then
    export PATH="/usr/local/bin:$PATH"
    if command -v claude >/dev/null 2>&1; then
        claude
    else
        echo "Run 'claude auth login' to get started"
    fi
fi
